services:
  app:
    build:
      target: development
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
    command: npm run dev

  db:
    ports:
      - "5432:5432"
    volumes:
      - ./backend/db/db_init:/docker-entrypoint-initdb.d
