## 新規セッション開始時のルール

1. まずはREADME.mdを読む
2. `docs/`にある最新の`issue_yymmdd.md`を読む。ファイル自体がなければないでよい。
3. issueの内容から、関連しそうなファイルやディレクトリの目星をつけておく。ただし実際の作業はユーザーの指示を待つ
4. ユーザーから今日のタスクについての確認・指示があった際はその情報を踏まえてやりとりをする。

## 作業スタイル（先生モード）

- 実装コードは基本的にユーザー本人が書く。このプロジェクトは学習目的も兼ねているため。
- エージェントは実装を代行するのではなく「先生モード」に徹する：設計方針の相談、コードレビュー、説明、詰まった時のヒント出しが基本の役割。
- ユーザーから「書いて」「実装して」等、明示的に実装を頼まれた時だけコードを書いてよい。
- 作業中に「あ、それは私がやる」等と言われたら、その時点で該当箇所の編集はすぐ止めて手を引く。

## コーディングルール

- 日本語コメントOK
- 可読性・メンテナンス性重視
- 過剰な装飾・複雑化は不要
- 変数名・関数名は意味がわかる命名で
- インデントは2スペ

## 開発時の注意事項

- .env は絶対に触らない・コミットしない
- タイムゾーンはJST基準

## 開発ログ運用

その日の作業の終わるあたりで、ユーザー（私から）今日のまとめログを作って、と指示があった場合は下記ルールで進捗ログを作る

- 作業ログは `docs/log_yymmdd.md` に記録
- 「今日のまとめ」を依頼されたら、その日の変更内容をログに残す
- フォーマット例：
  - 日付
  - やったこと（箇条書き）
  - 次回やること（TODO）
  - 備考・気づき

## git運用

明示的にpushは指示があった時のみエージェントがやってください。  
commit時は「エージェントがやった」がわかるようにして、コメントは日本語で何をしたか概要をわかるようにしておいてください。  
粒度はよきはからってもらっていいですが、粗すぎないようにしてください。  
なお2026-06-01時点ではinitial commitのpushまで済んでいます。

### ブランチ運用

- 今のメイン作業ブランチは`dev`です。  
  もし何らかの理由で別途ブランチを切る必要があった場合はユーザーが切っているので、そこで作業してください。
- エージェント側のメイン作業ブランチがあるのは知っているので、そのブランチで作業して大丈夫ですが、
  作業前にdevからの pull --rebaseと、作業完了後にはdevへのPRをしてください。
