# 作業ログ（2026-08-04）

DB設計ドキュメントを、現在のmigrationと確定済み仕様に合わせて整理した。
あわせて、GitHub Issue #1 のレビュー項目の対応状況を再確認した。

## やったこと

### 1. Issue #1 の対応状況の再確認

`a8504dd` 時点で、Issue #1 の指摘に対する対応状況を確認した。

| 項目 | 状態 |
|---|---|
| B-1 `auth.users` → `public.users` 作成trigger欠落 | 対応済み（`004_create_auth_user_trigger.sql`） |
| H-1 `authenticated` へのGRANT不在 | 対応済み（`005_grant_table_privileges.sql`） |
| M-1 `db_test/doc/RLS.md` が0バイト | 空ファイルは削除済み。参照リンクの設置は未対応 |
| M-2 スキーマ修飾 | `001` は対応済み。`002` は当初未対応（本日修正） |
| M-3 Supabase固有依存 | 未対応（後続フローで対応予定） |
| L-1 FK未index | 対応不要と判断済み |
| L-2 ファイル名コメント | 対応済み |
| L-3 公開viewerの終了済み植物 | 対応済み（`003:91`） |
| L-4 status / updated_at | 対応済み（作業キュー仕様として明文化） |
| T-1〜T-5 実行テスト | 未実施 |

B-1 の実装は、Issueの修正案（`search_path = public`）ではなく
`SET search_path = ''` + `INSERT INTO public.users` の完全修飾になっており、
Supabase推奨のより安全なパターンになっている。

### 2. Issue #1 への追加コメント投稿

本体レビューでカバーされていなかった項目を Issue #1 にコメントとして追加した。
https://github.com/s-kogure/niwa_log/issues/1#issuecomment-5174889083

- N-1 `002_enable_rls.sql` がスキーマ未修飾（M-2の対象漏れ）
- N-2 RLS.md 7.5「未決定事項」が L-3 対応後も残存
- N-3 `plants_Db_.md` 内で plant_master_requests 仕様が二重化・矛盾
- N-4 設計ドキュメントに記載があり `001` に存在しないカラム
- N-5 `weather_cache` の一意制約がドキュメントと不一致
- N-6 RLSテストケース #23 の期待結果が `005` のGRANT設計と噛み合わない
- N-7 `set_updated_at()` に `SET search_path` がない
- N-8 `plant_master_requests` のコメントアウトが残存

M-3 およびSupabase実行系（T-1〜T-5、GRANT実測）は後続フローのため、コメントの対象外とした。

### 3. SQL修正（※Claude以外の作業）

並行して以下が修正された。N-1 / N-7 が解消。

- `db_test/002_enable_rls.sql`：全10文に `public.` 修飾を追加
- `db_test/001_create_table.sql`：`set_updated_at()` に `SET search_path = ''` を追加

### 4. DB設計ドキュメントの整理

SQL・migration・アプリコードは変更せず、ドキュメントのみを対象とした。

#### `docs/db_settings/RLS.md`

- §7.5「未決定事項」節を削除し、確定仕様として業務仕様へ移動
- §7.5 公開閲覧条件に `AND user_plants.deleted_at IS NULL` を反映
  - `deleted_at` 判定は公開閲覧条件のみに置き、所有者条件には含めない旨を明記
- §7.5 業務仕様に「`user_plants` は `user_id` を持たず `gardens.owner_id` 経由で判定」を追加
- §7.10 業務仕様を作業キュー仕様へ統一
  - 行の存在＝pending、`status` / `updated_at` / `approved_plant_id` / `created_from_request_id` は不使用
  - SELECTは0件ではなく `permission denied` となる旨を明記
- §8 テストケース #20・#23 の期待結果を `permission denied`（GRANT無し）へ修正
  - 冒頭に「0件 / 拒否 / permission denied」を区別する表を追加
  - #12 の確認時に終了済み植物の表示可否をあわせて確認する補足を追加
  - #1 は `anon` へのGRANT有無で結果が変わる点を補足
- §2.1「RLSとテーブル権限（GRANT）の関係」を新設
  - `005_grant_table_privileges.sql` の付与内容を表で明記
  - ドキュメントにGRANT層の記述が皆無だったため追加
- §1 にmigrationファイル5本の対応表を追加
- §9 チェックリストにGRANT関連2項目と終了済み植物の非表示を追加

#### `docs/db_settings/plants_Db_.md`

- 冒頭の旧 plant_master_requests 仕様を削除し、作業キュー仕様へ統一
  - 旧記述：`status=approved` にする / `approved_plant_id` で紐づけ / v1では削除しない /
    plants側に `approved_at`・`created_from_request_id` を持たせる
- 2箇所に分散していた user_plants の記述を1つに統合
  - 「水やり係数にかかわる項目は太字」は表の「水やり計算」列として保持
- `user_id` を「v1では持たない／`gardens.owner_id` 経由」へ更新
- watering_logs の `amount` / `memo` / `updated_at` を「v1では持たない」へ更新
- `watch_dry_out` を「持つ項目」から「v1では持たない項目」へ移動
- weather_cache を現行SQLに合わせて更新
  - 一意制約を `(region_id, target_at, data_type)` へ
  - 列名を修正（`target_date`→`target_at`、`condition`→`weather_condition`、
    `temperature`→`temperature_c`、`humidity`→`humidity_percent`、
    `rainfall_mm`→`precipitation_mm`、`data_type` を追加）
  - `raw_json` を「v1では持たない」へ
- `plant_type` → `plant_category` に統一（SQLの列名に合わせる）
- `growth_stage` / `health_status` の値をSQLのCHECK制約に合わせて補完（`fruiting`、`unknown`）
- 全項目に「実装済み / v1では持たない / 未確定」の区分を付与
- 末尾に「未確定項目の一覧」を追加
- 全体のエスケープ記号（`\#\#`、`\-`、`\*\*`）でMarkdownが崩れていたため整形し直し
  - 差分が大きいのはこのため

#### SQLに実装済みでドキュメントに記載がなかったため追加したもの

新しい仕様は追加せず、実装内容をそのまま記述する形にとどめた。

- RLS.md §2.1 のGRANT表（`005_grant_table_privileges.sql` と1対1）
- plants_Db_.md の `observation_logs設計`（`001` の列定義と RLS.md 7.8 の既存記述のみ）

## 判断せず保留した項目

確定済み仕様に含まれていなかったため、削除せず「未確定」として残した。
`plants_Db_.md` 末尾の「未確定項目の一覧」に集約している。

| テーブル | 項目 | 論点 |
|---|---|---|
| plants | `avoid_overwatering` | 元ドキュメントで「持つ項目」と明記。`watch_dry_out` とペアの記述だったが、確定しているのは `watch_dry_out` のみ |
| plants | `evidence_confidence` | 初期データの調査根拠を管理するか |
| plants | `approval_status` / `approved_at` | 作業キュー方式なら「存在＝承認済み」で不要と整理できるが、確定仕様は `plant_master_requests` 側の `status` にしか言及がない |
| user_plants | `zone_id` / `zone_label` | READMEの④データモデルには `zones` テーブルの記載あり |
| user_plants | `location_type` | 元から「後続候補」表記 |
| user_plant_tags | `tag_type` | `001` は `user_plant_id` / `name` / `created_at` のみ |
| watering_logs / observation_logs | 自由記述の置き場所 | 「水やり理由などは memo に自由記述で残す」が `watering_logs.memo` 不採用と衝突 |
| weather_cache | `source` の扱い | 複数取得元を使う場合、一意制約に含めるか |

## SQLとdocsに残っている不一致

1. **README.md がSupabase前提になっていない**
   ③技術スタックが「Node.js + Express + PostgreSQL」、⑤が自前JWT認証のままで、
   `auth.users` へのFK・`auth.uid()`・`authenticated` ロールと矛盾（Issue #1 M-3）
2. **README.md ④データモデル**
   `zones` / `tags` / `user_roles` が記載されているが `001` に存在せず、
   逆に `observation_logs` / `plant_master_requests` が未記載
3. **同名・類似内容の設計ドキュメントが複数存在**
   `docs/db_settings/niwa_log_plants_db_clean.md`、
   `docs/specs_memo/niwa_log_db_design_v0.2.md`、
   `要件定義資料/plants_Db_.md`（いずれも今回未確認）
4. **`db_test/` 単体でガイドラインに辿り着けない**（Issue #1 M-1の残り）
   SQLへのコメント追加が必要なため未着手

## 次回やること（TODO）

- [ ] 本日のドキュメント変更の内容確認とコミット
- [ ] 保留項目（未確定8件）の要否判断
- [ ] Supabaseでのmigration実行（Issue #1 M-3 / T-1〜T-5）
  - [ ] T-1 `auth.users` 作成で `public.users` が自動生成されるか
  - [ ] T-2 `authenticated` へのGRANT・`search_path`・authスキーマ権限が実環境で成立するか
  - [ ] T-3 `visible_parent` 系Policyのサブクエリ内RLS伝播を2ユーザー＋公開/非公開庭で実測
  - [ ] T-4 UPDATEでのFK付け替え拒否（テスト#9）
  - [ ] T-5 `anon`（未ログイン）が全10テーブルにアクセス不可であること
- [ ] `anon` へのGRANT実測（Supabase既定privilegeの確認）
- [ ] README.md のSupabase前提への更新（M-3）
- [ ] `db_test/` からガイドラインへの参照リンク設置（M-1の残り）
- [ ] N-8 `plant_master_requests` のコメントアウト整理
- [ ] 実測結果が揃った時点で Issue #1 のクローズ判断

## 備考・気づき

- Issue #1 の M-2 は「`001`（無修飾）vs `003`（修飾済み）」の比較だったため、
  `002` が両者の視界から漏れていた。レビュー対象の切り出し方によって
  同種の問題が見落とされることがある
- RLS.md 7.5 の「未決定事項」は、`003` 側で実装が決着した後もドキュメントに残っていた。
  この状態でガイドライン通りに再実装すると L-3 の修正が失われる構造だった
- RLS（行の制御）とGRANT（操作の可否）は別レイヤーであり、
  期待結果も「0件 / 拒否 / permission denied」で異なる。
  テストケースはこの3つを区別して確認する必要がある
- ドキュメント整理では「現行SQLにない＝廃止」と短絡せず、
  確定済み仕様に含まれない項目は「未確定」として残す方針をとった
