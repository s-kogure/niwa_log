# 共有前チェック（2026-07-29）

対象は、現在の作業ツリーと Git の追跡・除外設定。SQL の内容や未保存のDB状態は変更していない。

## 結論

- すでに Git 管理されているファイルには、講師共有で明確に問題になる秘密情報・個人情報は見つからなかった。
- 実データの `.env` は ignore されている。
- ただし `.gitignore` 自体も ignore されており、現状の除外設定は GitHub へ push されない。共有リポジトリ側では ignore が効かない状態になる。
- `git add -A` を実行すると、現在未追跡の `db_test/` 以下（練習SQL、Claude/Codexレビュー、メモ）などが追加される。講師に見せたくない候補として最優先。

## 現在の Git 管理対象

追跡済みは17ファイル。アプリの骨組み、DBマイグレーション、`.env.example`、`README.md`、`docker-compose.dev.yml` のみ。

- `backend/db/migrations/001_users.sql` ～ `010_user_roles.sql`：本番用マイグレーションとして共有対象。
- `backend/db/migrate.js`、`backend/src/index.js`、`backend/Dockerfile`、`backend/package.json`：共有対象。
- `.env.example`：接続設定の項目を含むが、`POSTGRES_PASSWORD` と `JWT_SECRET` はプレースホルダー形式。実値ではないことを確認。
- `docker-compose.dev.yml`：追跡済み。`.gitignore` にも書かれているが、すでに追跡済みのため ignore 指定は効かない。

テキストを秘密情報・メールアドレス・電話番号のパターンで確認した範囲では、追跡済みファイルに実値の認証情報や個人情報は検出されなかった。DB関連の `password` / `secret` という語は設定項目名・スキーマ記述として出現している。

## `git add -A` で追加される未追跡ファイル

現在の未追跡エントリは次の5件。これらは `.gitignore` の対象外なので、追加操作で共有対象になる。

1. `db-practice-tutorial.md`
2. `db_test/`
   - `001_create_table.sql`、`002_enable_rls.sql`、`003_create_policies.sql`
   - `doc/claude_codeレビュー.md`、`doc/codexレビュー.md`、`doc/memo.md` ほか
3. `docker-compose.yml`
4. `icon_ai/`（PNG / SVG / Adobe Illustrator `.ai` の下書き）
5. `要件定義資料/`（PDF、DOCX、ペルソナ・設計メモ等）

特に `db_test/doc/claude_codeレビュー.md`、`db_test/doc/codexレビュー.md`、`db_test/doc/memo.md` は、ファイル名からして作業過程が見えるため「講師に見られてはずかしいか」の確認対象。`icon_ai/` の `.ai`、`要件定義資料/` の DOCX/PDF は作成者などの文書メタデータを含む可能性がある。

## ignore 設定の実効状態

現在の `.gitignore` は次を除外している。

- `docs/`、`design_comp/`、`README.local.md`、`CLAUDE.md`
- `docker-compose.dev.yml`、`node_modules/`
- `.gitignore`、`icons/`、`icons_ai/`
- `.DS_Store`、`.env`、`.env.example`

実際に ignore されていることを確認できたもの：`.env`、`docs/`、`design_comp/`、`README.local.md`、`CLAUDE.md`、`.DS_Store`。

注意すべき不整合：

- `.gitignore` が ignore されているため、未追跡のまま。push されず、共有先でこのルールは再現されない。
- `.env.example` も ignore 指定されているが、すでに追跡済みなので push される。
- `docker-compose.dev.yml` も同様にすでに追跡済みなので push される。
- `docs/` を丸ごと ignore しているため、このファイル `docs/issues/issue_260729.md` も作成後は Git 追加されない。

## DBファイル・ダンプ

リポジトリ内に `*.db`、`*.sqlite`、`*.sqlite3`、`*.dump`、`*.bak` は見つからなかった。Docker のPostgreSQLデータは named volume `postgres_data` を使う構成で、リポジトリ内にDB実体を置く設定ではない。

## 作業ツリーの状態

- 追跡済みファイルの変更：0件
- 未追跡エントリ：5件（上記）
- Git の差分チェック：エラーなし
