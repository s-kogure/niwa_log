# 2026-09-10 作業ログ

## やったこと

- Phase 5 item 5-3「DB接続共通化」完了
  - `backend/src/db.js`を新規作成：共通の`pg.Pool`を1箇所に集約、`pool.on('error', ...)`でアイドルコネクション異常時の安全対策も追加
  - `backend/db/migrate.js`を、独自に`pg.Pool`を生成する実装から`db.js`の共有プールを使う実装へ書き換え
  - `backend/src/config.js`に`import 'dotenv/config'`を追加し、env読み込みをconfig.js側へ集約
- `.gitignore`のバグ修正
  - 9/4時点の変更で`backend/db`ディレクトリ丸ごとが除外パターンになっており、`migrate.js`（実コード）まで誤って追跡対象外になっていたことを発見
  - `backend/db/migrations`のみを除外する形に修正（あわせて行頭の余分な半角スペースによるパターン不一致も修正）
- `CLAUDE.md` / `AGENTS.md`に「作業スタイル（先生モード）」を明文化
  - 実装は基本ユーザー本人が書き、Claudeは相談・レビュー・ヒント出し役に徹する運用を明文化
  - `AGENTS.md`を新規作成（CLAUDE.mdの内容をミラー、Codex等の他エージェント向け）
- 動作確認中に既存の不整合を発見
  - `schema_migrations`テーブルには2026-06-01時点の適用記録（`001_create_users.sql`等）が残っている一方、現在の`backend/db/migrations/`内のファイル名は`create_`が抜けた名称（`001_users.sql`等）にリネームされており、ファイル名不一致により`migrate.js`が全ファイルを「未適用」と誤判定→既存テーブルへの再CREATEで失敗することを確認
  - GitHub issue #8にコメントで記録し、item 5-4（マイグレーション実行機構）のスコープとして先送り
- ガントチャート（Google Sheets）を確認し、Phase 5の進捗を把握
  - 5-1・5-2・5-3が完了、5-4「マイグレーション実行機構」が次の壁

## 次回やること（TODO）

- item 5-4「マイグレーション実行機構」に着手
  - `backend/db/migrations/`（旧設計）と`db_test/`（確定版）のどちらを正とするか確定
  - ファイル名不一致の解消、開発環境の`postgres_data`ボリュームをリセットしてクリーンな状態から再検証
- item 5-5「Express基本構成」の続き
- `pool.on('error', ...)`は起動確認のみ実施。実際にエラーを意図的に発生させた動作確認はまだ

## 備考・気づき

- 「先生モード」の運用ルールが実は明文化されてなかったことに気づき、今回`CLAUDE.md`/`AGENTS.md`に追記。ユーザー本人談「重大設定なのにわすれてたｗ」
- `.gitignore`のパターンミス（除外範囲が広すぎる／行頭スペース）で、せっかくの実装がcommitされない事故になりかけた。地味だけど見落としやすいポイント
- コミットメッセージ作成時、シェルで`\n`を書いても改行にならない（ヒアドキュメントか`-m`複数指定が必要）という沼にもハマった
