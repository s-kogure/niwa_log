# 2026-09-04 作業ログ

## やったこと

- Phase 5「バックエンド基盤整備」の親issue（[#8](https://github.com/s-kogure/niwa_log/issues/8)）を作成
  - README記載の8項目をスコープとして整理、必要に応じて子issueを切り出す運用に決定
- `.gitignore`を修正：`backend`（全体除外）→`backend/db`（migrationsのみ除外）
  - `backend/src`・`package.json`・`Dockerfile`等は追跡対象に
- item 1「Docker / Compose確認」完了
  - `docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build`でapp/db起動確認
  - dbのhealthcheck→app起動の順序、`/health`エンドポイントの応答、nodemonによるホットリロードを確認
- item 2「環境変数設定」完了
  - `backend/src/config.js`を新規作成：必須環境変数（`POSTGRES_HOST`/`PORT`/`DB`/`USER`/`PASSWORD`）の起動時チェック＋`config`オブジェクトのexport
  - `backend/src/index.js`を`config.js`経由でPORTを参照するよう変更
- 副産物：`backend/package.json`の`bcryptjs`・`jsonwebtoken`、`.env.example`の`JWT_SECRET`が、確定仕様（Supabase Auth）以前の自前認証設計の名残と判明
  - issue #8にコメントでメモ残し、item 7着手時に要否見直すことに
- 動作確認中に`.env`と`.env.example`の内容不一致（`POSTGRES_HOST`/`POSTGRES_PORT`）を発見・修正
  - あわせて「`.env`の変更はコンテナ再作成しないと反映されない（nodemonのファイル監視とは別物）」を実地で確認

## 次回やること（TODO）

- item 3「DB接続共通化」に着手
- item 4「migration実行機構」着手時、`backend/db/migrations/`（旧設計）と`db_test/`（確定版）の内容差異を解消
- item 7「認証ミドルウェア」着手時、Supabase AuthのJWT検証方式（自前secret検証 / JWKS / API委譲）を確定し、`bcryptjs`・`jsonwebtoken`・`JWT_SECRET`の要否を見直す

## 備考・気づき

- `docs/logs/`サブフォルダを新設。以後の作業ログはここに集約（過去分の`docs/log_260601.md`は未移動）
- 環境変数まわりの「required配列→forチェック→config export」の実装は、ばぶ先生タイム込みで自力で書けた回
