# 2026-09-11 作業ログ

## やったこと

- Phase 5 item 5-4「マイグレーション実行機構」完了
  - `db_test/`（確定版）と`backend/db/migrations/`（旧設計）の不整合を解消。方針として`db_test/`を正式migrationとして採用し、内容そのまま`backend/db/migrations/`へ移行（rename、SQL自体は無変更）
  - `.gitignore`の`backend/db/migrations`除外を解除し、git管理下に
  - ローカルDocker環境でmigrationを実行すると、Supabase依存部分が存在せず段階的にエラーが発生。1つずつ原因を特定して解消
    - `schema "auth" does not exist` → `auth`スキーマ・`auth.users`（`id`列のみの最小スタブ）を追加
    - `role "authenticated" does not exist` → `anon`/`authenticated`/`service_role`ロール（`nologin`）を追加
    - `function auth.uid() does not exist` → Supabase本家と同じ実装（`current_setting('request.jwt.claim.sub', true)`）でスタブ作成
    - `role "postgres" does not exist` → ローカル`.env`の`POSTGRES_USER`を`niwa`から`postgres`に変更（`006_revoke_default_privileges.sql`が`FOR ROLE postgres`を決め打ちしているため、本番Supabase・`.env.example`と同じユーザー名に統一）
  - これらのスタブは`backend/db/db_init/001_stub_auth.sql`としてまとめ、`docker-entrypoint-initdb.d`経由（`docker-compose.dev.yml`でボリュームマウント）でローカル限定実行される形にし、本番相当のmigration本体は一切汚さない構成にした
  - PostgreSQL 17（Docker）で`001`〜`010`の通し実行を確認。`regions`（1,747件）・`plants`（107件）のseed投入も確認済み
  - commit `abd6c81`として`dev`へpush済み
- 作業中に判明した`docker compose`の仕様
  - `docker compose up`単体では`docker-compose.dev.yml`は自動読み込みされない（`docker-compose.override.yml`という名前だけが自動合体対象）。`-f docker-compose.yml -f docker-compose.dev.yml`と明示指定する必要がある
- GitHub issue #8を更新
  - スコープの「migration実行機構」にチェック
  - 完了報告と次セッションへの申し送りをコメントで記録

## 次回やること（TODO）

- item 5-5「Express基本構成」に着手
- README.mdの更新（「migration / seed」節・リポジトリ内資料一覧が`db_test/`前提の記述のまま）
- `_bk_db_test/`（旧`db_test/`のローカルバックアップ、git管理外）の削除判断
- 次イテレーション課題：`schema_migrations`にファイル内容のハッシュを保存し、適用済みファイルの改変・リネームを検知する仕組み（今回は未着手）

## 備考・気づき

- 「先生モード」がフルに機能した回。実装コードは全部ユーザー本人がコピペ・タイピングし、Claudeは原因の切り分けと次の一手の提示に徹した
- `docker compose exec app npm run migrate`のエラーメッセージが1回ごとに1歩ずつ変わっていく展開になり、Supabase依存部分（`auth`スキーマ→ロール→関数→ロール名）を1つずつ剥がしていく過程がそのままログに残った
- commitメッセージの箇条書きが改行なしで1行につながる問題が再発（9/10にも一度ハマった沼）。ヒアドキュメントか`-m`複数指定でないと改行が効かない、というのを再確認
